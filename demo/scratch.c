#include <stdio.h>

int main() {
    int data = 5;
    int result = 0;

    if(data > 3);
    {
        data++;
        result = data;
    }

    printf("data: %d, result: %d\n", data, result);

    return 0;
}